{"config":{"lang":["en"],"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"RoValra Contributor Documentation (RoValra CD)","text":"","path":["RoValra Contributor Documentation (RoValra CD)"],"tags":[]},{"location":"#contents","level":2,"title":"Contents","text":"<ul> <li> <p>Reading Settings</p> <ul> <li>Unified API</li> <li>Chrome Storage</li> <li>Load Settings</li> </ul> </li> <li> <p>Locales</p> <ul> <li>Reading Locales</li> <li>Adding a Locale</li> </ul> </li> </ul>","path":["RoValra Contributor Documentation (RoValra CD)"],"tags":[]},{"location":"Locales/adding-locale/","level":1,"title":"Adding a Locale","text":"<p>To add a locale, first create the locale file under <code>public/Assets/locales/&lt;language_code&gt;.json</code>. Make sure it follows the overall structure of the English locale file.</p> Tip <p>If a locale isn't found in your translation, i18next will automatically fallback to the English locale.</p> <p>Once you've added the locale file, you'll want to modify the <code>rovalraLanguage</code> setting in <code>core/settings/settingConfig.js</code> to add your language as an option, in <code>options: [...]</code>. Add your language as:</p> <pre><code>{ label: '&lt;Language&gt; (&lt;Language name in said language&gt;)', value: '&lt;language code&gt;' },\n</code></pre> <p>Example:</p> <pre><code>{ label: 'Romanian (Română)', value: 'ro' },\n</code></pre>","path":["Locales","Adding a Locale"],"tags":[]},{"location":"Locales/adding-locale/#adding-yourself-as-a-translator","level":2,"title":"Adding yourself as a Translator","text":"<p>To actually get the translator badge and show up in the contributor list:</p> <ol> <li>First, add yourself to <code>src/content/core/configs/userIds.js</code> to <code>TRANSLATOR_USER_IDS</code>. This will give you a translator badge</li> <li>Create a locale under <code>settings.credits.otherContributions.locales</code> with a short description stating that you made a locale for that specific language. You will need this in the next step.</li> <li>Add yourself to <code>src/content/core/configs/otherContributions.ts</code> to <code>OTHER_CONTRIBUTIONS.Locales.contributors</code> as <code>new Contribution(&lt;roblox user ID&gt;, \"locales.&lt;rest of locale key&gt;\", \"&lt;Pull Request link (optional)&gt;\")</code>. Take the other entries as examples if you need to.</li> </ol>","path":["Locales","Adding a Locale"],"tags":[]},{"location":"Locales/reading-locale/","level":1,"title":"Reading Locales","text":"","path":["Locales","Reading Locales"],"tags":[]},{"location":"Locales/reading-locale/#reading-a-locale-without-variables","level":2,"title":"Reading a locale without variables","text":"<code>JavaScript</code><code>TypeScript</code> <pre><code>import { t } from \"../core/locale/i18n.js\";  // (1)!\n\nasync function main() {\n    const message = await t(\"helloMessage\");\n\n    console.log(message);\n}\n</code></pre> <ol> <li>Update path accordingly</li> </ol> <pre><code>import { t } from \"../core/locale/i18n.js\";  // (1)!\n\nasync function main() {\n    const message: string = await t(\"helloMessage\");\n\n    console.log(message);\n}\n</code></pre> <ol> <li>Update path accordingly</li> </ol> Tip <p>Trying to read a locale that doesn't exist neither in the current selected translation nor in the fallback (english) one will return the target key (ex. <code>\"helloMessage\"</code>).</p>","path":["Locales","Reading Locales"],"tags":[]},{"location":"Locales/reading-locale/#reading-a-locale-with-variables","level":2,"title":"Reading a locale with variables","text":"<p>If a locale is defined as:</p> <pre><code>{\n    \"helloMessage\": \"Hello, {{name}}!\"\n}\n</code></pre> <p>Then, you need to give it a value for <code>name</code>:</p> <code>JavaScript</code><code>TypeScript</code> <pre><code>import { t } from \"../core/locale/i18n.js\";  // (1)!\n\nasync function main() {\n    const message = await t(\"helloMessage\", { name: 'John Doe' });\n\n    console.log(message);\n}\n</code></pre> <ol> <li>Update path accordingly</li> </ol> <pre><code>import { t } from \"../core/locale/i18n.js\";  // (1)!\n\nasync function main() {\n    const message: string = await t(\"helloMessage\", { name: 'John Doe' });\n\n    console.log(message);\n}\n</code></pre> <ol> <li>Update path accordingly</li> </ol>","path":["Locales","Reading Locales"],"tags":[]},{"location":"Locales/reading-locale/#reading-nested-locales","level":2,"title":"Reading nested locales","text":"<p>If a locale is defined as:</p> <pre><code>{\n    \"messages\": {\n        \"helloMessage\": \"Hello, world!\"\n    }\n}\n</code></pre> <p>Then, you need to type out the entire key, with <code>.</code> as the separator:</p> <code>JavaScript</code><code>TypeScript</code> <pre><code>import { t } from \"../core/locale/i18n.js\";  // (1)!\n\nasync function main() {\n    const message = await t(\"messages.helloMessage\");\n\n    console.log(message);\n}\n</code></pre> <ol> <li>Update path accordingly</li> </ol> <pre><code>import { t } from \"../core/locale/i18n.js\";  // (1)!\n\nasync function main() {\n    const message: string = await t(\"messages.helloMessage\");\n\n    console.log(message);\n}\n</code></pre> <ol> <li>Update path accordingly</li> </ol> <p>Alternatively, you can also read the entire <code>\"messages\"</code> key, though doing so is not preferred:</p> <code>JavaScript</code><code>TypeScript</code> <pre><code>import { t } from \"../core/locale/i18n.js\";  // (1)!\n\nasync function main() {\n    const message = await t(\"messages\");\n\n    console.log(message.helloMessage);\n}\n</code></pre> <ol> <li>Update path accordingly</li> </ol> <pre><code>import { t } from \"../core/locale/i18n.js\";  // (1)!\n\nasync function main() {\n    const message: Record&lt;string, any&gt; = await t(\"messages\");\n\n    console.log(message.helloMessage);\n}\n</code></pre> <ol> <li>Update path accordingly</li> </ol>","path":["Locales","Reading Locales"],"tags":[]},{"location":"Reading%20Settings/chrome-storage/","level":1,"title":"Chrome Storage","text":"<p>To read settings with <code>chrome</code> APIs, run the following:</p> content/core/settings/settingConfig.js<pre><code>export const SETTINGS_CONFIG = {\n    Category: {\n        ...,\n\n        settings: {\n            settingName: {\n                ...,\n\n                default: 'foo',\n            },\n\n            ...\n        },\n    },\n</code></pre> <code>JavaScript</code><code>TypeScript</code> your-file.js<pre><code>const setting = (await chrome.storage.local.get({ settingName: 'foo' })) // (1)!\n    .settingName;\n</code></pre> <ol> <li>Please, in the Record passed to chrome.storage.local.get, make sure that the key is your setting key as it appears in <code>SETTINGS_CONFIG</code>, and that the value is your default value!</li> </ol> your-file.ts<pre><code>const setting: unknown =\n    (await chrome.storage.local.get({ settingName: 'foo' })).settingName;  // (1)!\n\nif (typeof setting !== 'string') {\n    throw new Error(\"Invalid type!\");\n}\n</code></pre> <ol> <li>Please, in the Record passed to chrome.storage.local.get, make sure that the key is your setting key as it appears in <code>SETTINGS_CONFIG</code>, and that the value is your default value!</li> </ol> <p>Deprecated</p> <p>This method has been deprecated in favor of the unified settings API, as the new alternative automatically applies caching.</p>","path":["Reading Settings","Chrome Storage"],"tags":[]},{"location":"Reading%20Settings/load-settings/","level":1,"title":"Load Settings","text":"<p>To read settings with <code>loadSettings</code>, do as follows:</p> <code>JavaScript</code><code>TypeScript</code> your-file.js<pre><code>import { loadSettings } from '../core/settings/handlesettings.js';\n\nasync function main() {\n    const settings = await loadSettings(); // (1)!\n}\n</code></pre> <ol> <li>Each key in <code>settings</code> corresponds directly to a setting key from <code>SETTINGS_CONFIG</code></li> </ol> your-file.ts<pre><code>import { loadSettings } from '../core/settings/handlesettings.js';\n\nasync function main() {\n    const settings: Record&lt;string, unknown&gt; = await loadSettings();  // (1)!\n}\n</code></pre> <ol> <li>Each key in <code>settings</code> corresponds directly to a setting key from <code>SETTINGS_CONFIG</code></li> </ol> <p>Deprecated</p> <p>This method has been deprecated in favor of the unified settings API, as the new alternative automatically applies caching. In contrast, this method always loads all settings on each call, without caching. However, this method and the chrome API remain usable in contexts in which the unified settings API may not be available.</p>","path":["Reading Settings","Load Settings"],"tags":[]},{"location":"Reading%20Settings/unified/","level":1,"title":"Unified Settings API","text":"<p>To read a setting using the unified settings API, do:</p> <code>JavaScript</code><code>TypeScript</code> your-file.js<pre><code>import { settings } from '../core/settings/getSettings.js';\n\nasync function main() {\n    const setting = await settings.settingKey;  // (1)!\n}\n</code></pre> <ol> <li>Replace <code>settingKey</code> with your setting's key as it appears in <code>SETTINGS_CONFIG</code></li> </ol> your-file.ts<pre><code>import { settings } from '../core/settings/getSettings.js';\n\nasync function main() {\n    const setting: unknown = await settings.settingKey;  // (1)!\n}\n</code></pre> <ol> <li>Replace <code>settingKey</code> with your setting's key as it appears in <code>SETTINGS_CONFIG</code></li> </ol> <p><code>settings</code> will automatically update when any setting is changed, and also automatically loads all settings once and caches them until the next mutation to the settings.</p>","path":["Reading Settings","Unified Settings API"],"tags":[]}]}